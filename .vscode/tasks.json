{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cppbuild",
            "type": "shell",
            "command": "${workspaceFolder}/buildwithCMake.bat",
            "group": "build"
        }
    ]
}